AI Engineer roadmap scraper

Prerequisites
- Python 3.8+

Setup

```bash
python -m pip install -r requirements.txt
python -m playwright install
```

Run

```bash
python scraper.py
```

Output
- `output/ai_engineer.json` — scraped data (title, text, links)
